name: Supabase Migration

on:
  push:
    branches: [main]
    paths:
      - "supabase/migrations/**"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry Run (zeigt SQL ohne Ausführung)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

# Don't cancel in-progress migrations — they modify the database
concurrency:
  group: migrate-${{ github.ref }}
  cancel-in-progress: false

env:
  SUPABASE_PROJECT_REF: "vrodyamhdxrspnaxzdtg"

jobs:
  migrate:
    name: Apply Supabase Migrations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v2
        with:
          version: latest

      - name: Link project
        run: supabase link --project-ref ${{ env.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Dry run (show pending migrations)
        if: ${{ github.event.inputs.dry_run == 'true' }}
        run: supabase db push --dry-run
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Apply migrations
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
