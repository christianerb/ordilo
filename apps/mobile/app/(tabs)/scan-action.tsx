/**
 * Placeholder route for the center scan tab. The tab bar button intercepts
 * the press and opens the /scan modal instead, so this screen never
 * renders — it only exists because every Tabs.Screen needs a route.
 */
export default function ScanActionPlaceholder() {
  return null;
}
