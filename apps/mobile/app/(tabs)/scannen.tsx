/**
 * Placeholder route for the dock's Scannen slot. Never rendered — the
 * OrdiloTabBar intercepts the press and opens the scan modal directly.
 */
export default function ScannenTabPlaceholder() {
  return null;
}
